name: home
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: 1.0.0
  common:
    path: ../../common
  add_new_card:
    path: ../add_new_card
  scroll_snap_list: ^0.8.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner: 2.1.5
  freezed: 1.0.0

flutter:
